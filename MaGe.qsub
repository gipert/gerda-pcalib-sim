#!/bin/bash
#
#$ -P short
#$ -j y
#$ -cwd
#$ -p -1
#$ -o /dev/null
#$ -e /dev/null

macro=${1}-${SGE_TASK_ID}.mac

singularity exec gerda-sw-all_active.sif /usr/bin/time -v \
    MaGe "$macro" &> $(echo $macro | cut -f 1 -d '.').out
